services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kosmio-front
    ports:  
    ## IMPORTANT : Définissez votre port local dans le fichier .env avec la variable VITE_FRONT_PORT!! 
      - "${VITE_FRONT_PORT:-5173}:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    restart: unless-stopped