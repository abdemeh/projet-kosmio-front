import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

const ficheSlice = createSlice({
  name: 'fiche',
  initialState,
  reducers: {
  },
});


export const { } = ficheSlice.actions;
export default ficheSlice.reducer;